# Personal data (user fills these)
.env
cv.md
article-digest.md
interview-prep/story-bank.md
config/plugins.yml
data/applications.md
data/search-ops.md
data/pipeline.md
data/scan-history.tsv
data/job-queue.json
data/job-queue.json.bak-*
data/job-queue.md
data/job-queue-archive.json
data/queue-ui-launch-state.json
data/gmail-state.json
data/gmail-organizer-state.json
data/jackandjill-recommendations.json
data/handshake-recommendations.json
data/handshake-sync-status.json
data/handshake-inbox.json
data/handshake-inbox-sync-status.json
data/application-ingest-state.json
data/application-import-review.json
data/jackandjill-board.json
data/jackandjill-archive-ledger.jsonl
data/jackandjill-coaching/
data/application-answer-drafts/
data/outreach-state.json
data/outreach-contacts.json
data/application-policy.json
data/application-question-ledger.json
data/application-runs.json
data/application-clear-state.json
data/application-clear.lock
data/application-handoff-session.json
data/civic-state.json
data/application-handoff.lock
data/.queue-health.lock
data/.job-queue.lock
data/cache/
plugins.local/
data/pdf-index.tsv
reports/*.md
!reports/.gitkeep
output/*
!output/.gitkeep
output/sheets/
batch/logs/*
!batch/logs/.gitkeep
batch/batch-state.tsv
batch/batch-input.tsv
batch/tracker-additions/*.tsv
!batch/tracker-additions/.gitkeep
batch/fast-pass/*
!batch/fast-pass/.gitkeep
jds/*
!jds/.gitkeep

# User config and customization (never auto-updated)
config/profile.yml
config/application-profile.json
config/google-sheets.json
portals.yml
modes/_profile.md
.update-dismissed
.update-lock
.manifesto-noted

# Generated
.resolved-prompt-*
node_modules/
package-lock.json
.update-incoming/
.scan-*.json
.scan-tmp.mjs
.playwright-mcp/

# OS
.DS_Store
*.mov
*.mp4

# Claude Code local
.claude/settings.local.json
.claude/memory/
career-dashboard

.quality-runner/

# Generated / cache / audit output
.aios/
.turbo/
.firecrawl/
.pytest_cache/
.agents/
__pycache__/

# Test coverage reports
coverage/

# Upstream v1.25 additions
local/
# Everything under data/ is generated or personal at runtime — only .gitkeep
# placeholders are tracked (verify with `git ls-files data/`). A blanket rule
# means a new generated file (e.g. a future log/cache addition) is covered
# for every user automatically, with no per-filename .gitignore update needed.
data/*
!data/.gitkeep
!data/offers/
!data/parser-output/
# Received offers/contracts, promise notes, prep reports (PII — never commit)
data/offers/*
!data/offers/.gitkeep
data/parser-output/**
!data/parser-output/**/
!data/parser-output/.gitkeep
!data/parser-output/**/.gitkeep
batch/tracker-additions/**/*.tsv
# Personal documents — never commit scans or copies of these (PII).
# Catches common identity/credential filenames so they can't be staged by accident.
*passport*
*diploma*
*简历*
*学位*
*成绩单*
# Interview prep (accumulated STAR+R stories — user data)
interview-prep/*
!interview-prep/
!interview-prep/.gitkeep
# Session transcripts hold real interviewer names/companies — ignore content,
# ship only the system-owned scaffold (README + .gitkeep). #1242
!interview-prep/sessions/
interview-prep/sessions/*
!interview-prep/sessions/.gitkeep
!interview-prep/sessions/README.md
# Writing samples (user's personal files — never committed)
writing-samples/*
!writing-samples/README.md
config/cv-facts.json
config/benchmarks.yml
modes/_custom.md
modes/_brief.md
# Plugin layer — opt-in toggles + user-installed plugins + integrity lock (never auto-updated)
plugins.lock
# Secrets (never commit — use .env.example as template)
# Universal safety net — protects ALL checkouts/branches, incl. untracked subprojects
# (e.g. web/ before it graduates to main). Never commit local env files or TS build caches.
**/.env.local
**/.env*.local
**/*.tsbuildinfo
bun.lock
# Report-number reservation sentinels (ephemeral, created by reserve-report-num.mjs)
reports/*-RESERVED.md
# Backup files written by normalize-statuses.mjs / dedup-tracker.mjs
*.bak
# OpenRouter runner — generated data (personal, never commit)
data/model-blacklist.json
# CLI-specific local config
.opencode/settings.local.json
.opencode/memory/
# OpenCode project config (often holds personal MCP/model/per-machine prefs).
opencode.json
career-dashboard.exe
!web/package-lock.json
# Playwright MCP server run artifacts (ephemeral, machine-local)
test-results/
playwright-report/
# Planning (local scratchpad)
plan/
# Web — runtime cache/history the dashboard writes locally; nothing here is tracked.
.career-ops-web/
# Upgrade-test fixtures are fictional system-layer data, never user data
# (see DATA_CONTRACT.md). Re-include them past the unanchored user-layer
# patterns above (cv.md, portals.yml).
!test-fixtures/**
