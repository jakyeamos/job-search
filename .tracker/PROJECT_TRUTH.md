# Career Ops Project Truth

## Current State

- Replaced the blunt experience-floor DQ with context-aware detection + a graduated response on 2026-07-25. New `experience-floor.mjs` requires an experience anchor near a year quantity and rejects negative context (vesting, PTO accrual, tuition, ROI/payback copy, company history, degree length, descriptions of future colleagues), clipping every context window to its sentence — two separate bugs came from cues leaking across boundaries. Aggregation differs by axis on purpose: within one range the LOW end binds (`1–4 years` admits a 1-yr candidate), across separate bullets the HIGH one binds (requirement bullets are conjunctive — UiPath's `5+ yrs Java` + `1+ yrs B2B` is a bar of 5). Required floors shadow preferred ones entirely. `scoreCandidate()` now grades per the user's policy (`modes/_profile.md` → Your Scoring Rules): ≤2 free, 3 costs 0.5, 4–5 costs 1.2 and blocks only when role substance also misses, ≥6 hard DQ — which required hoisting `targetMatch`/`backendSignal`/`aiDataSignal` above the blocker gate. Live queue: 168 items carry a real required floor (≤2: 62, 3: 48, 4–5: 39, ≥6: 19); hard DQs drop 131 → 19. The old `[3-9]` class was blind to double-digit floors and had let 4 genuinely senior reqs through (Slack/Salesforce Staff at 10, Sundayy at 12, Reserv at 10). New `rescore-queue.mjs` re-judges stored descriptions offline after any rule change — it preserves identity/history/`descriptionFetchedAt` and never reverses an `applied`/`skipped`/`snoozed`/`archived`/`stale` disposition; its run moved 60 excluded → ready, 22 excluded → in_review, 1 ready → excluded (Sundayy). 246/246 tests pass; `verify-pipeline.mjs` clean.
- Queue refills to 10 from the scored pool with a 4.0 floor (`selectDailyQueue`/`topUpSelection`); new six-column application board over the tracker. Board live at `/board.html`; daily selection holds ten roles at/above 4.0; commits `31c4580`…`a863779` (Tasks 1–8 of the refill+board plan).
- Closed the upstream cause of blind 3.7 scoring on 2026-07-25 so the backfill below stays a one-time cleanup. The fetching logic moved out of `backfill-descriptions.mjs` into a shared `posting-fetch.mjs` (LinkedIn guest endpoint + `resolveAtsApi` + `resolveEmbeddedGreenhouse`, all behind an injectable `fetchFn`), and `queue.mjs refresh()` now calls its `enrichCandidates()` between the gambling filter and `buildQueueItem`, so every description-less candidate gets its real title/company/location/description before it is ever scored. Enrichment was placed in core rather than the Gmail plugin deliberately — the plugin's guarded fetch would need `www.linkedin.com`/ATS hosts added to `manifest.json` `allowedHosts` and `plugins.lock` re-signed, and core placement covers jackandjill alerts too. Budget is 150 fetches/run at concurrency 2, alert-sourced candidates (`liveness: 'source-alert'`) served first so scanned candidates cannot eat the limit; a posting that returns 404/410 comes back `expired` and is dropped, a 429 is only `blocked` and stays retryable, and both the applications and expired filters are re-applied after enrichment because a corrected company can newly match an application. Fallback path hardened too: `parseRoleAtCompany` now cuts digest tails ("… and 7 more jobs in New York, NY for you. Apply Now."), drops CTA tails, and rejects any company over 60 chars or 6 words, and `plugins/gmail/index.mjs` no longer stamps a digest subject onto every URL in the message. `PROCESSING_VERSION` deliberately left at 7 — bumping it would reset `processed_message_ids` and re-ingest ~5000 messages. New `tests/posting-fetch.test.mjs` (14) and `tests/gmail-subject-parse.test.mjs` (7); 199/199 tests pass, `verify-pipeline.mjs` clean.
- Backfilled real job descriptions for the 307 queue items stuck at exactly `fitScore 3.7` on 2026-07-25 via the new `backfill-descriptions.mjs`, then rescored them. Those items were all `description: null` — 3.7 is precisely `2.8` base + `0.9` title match with no other signal, so they were scored blind and were mutually indistinguishable. Sources: LinkedIn's public unauthenticated guest posting endpoint (280 items; the health checker's `isRestrictedHealthUrl` LinkedIn no-crawl policy was left untouched), the existing `resolveAtsApi` ATS JSON APIs (7), and a new `resolveEmbeddedGreenhouse` `gh_jid` fallback for company-domain Greenhouse embeds like nuro.ai (9). 291 fetched, 3 expired → `stale`, 13 unfetchable (11 Glassdoor return HTTP 403 to any client; 1 seatgeek.com whose guessed Greenhouse board 404s; 1 malformed). Net movement: **133 → ready, 147 → excluded, 11 stayed in_review**. Newly visible blockers were dominated by the 3+-year experience floor (131) and defense/intel/clearance (38), plus 6 foreign-language gates the 2026-07-24 Celonis fix now catches. Queue integrity held: 1,282 items, 0 duplicate ids — `rescoreItem` explicitly preserves `id`/`source`/`discoveredAt`/`firstSeenAt` because `stableQueueId` hashes company+title and the backfill corrects the email-subject garbage that `gmail:linkedin` ingestion writes into `company`. Items rescored into `excluded`/`stale` are dropped from the daily slate. Pre-change backup at `data/job-queue.json.bak-1784988430292`; `verify-pipeline.mjs` clean; 185/185 tests pass.
- Evaluated Pylon "Infrastructure Engineer" — **2.0/5 SKIP**: 3+-yr backend/infra floor is a hard DQ, and the $180–280K band prices exactly that tenure. Same shape as the CrewAI SKIP — strengths land on the JD's Bonus/velocity bullets while its first three (own core infra, prevent incidents, scale) are the thin ones. Tracker #111, `reports/059-pylon-2026-07-25.md`; no PDF/answers/cover letter by design. Redirect to Pylon's SWE — AI Agents / AI Infra reqs. Pattern recorded in `research/experience-floor-lessons-2026-07.md`: 21/70 SKIPs (~30%) die on a YOE floor, clustered 3–5 yrs under non-senior titles that title filtering misses. Scanner fix shipped same day — see top entry.
- Evaluated Lightfield "Full-Stack Software Engineer" — **4.3/5**, apply; Apollo GraphQL is the one real gap, comp undisclosed so anchor $120–140K and walk below $105K. Tracker #109, `reports/055-lightfield-2026-07-24.md`, PDF `output/Jakye-Amos-Lightfield-Full-Stack-Engineer.pdf`. Not submitted.
- Evaluated Netic "Software Engineer, New Grad (Agent Platform)" — **4.8/5 APPLY**, the tightest agent-infra match in the pipeline (orchestration + eval harnesses + agents-authoring-agents map 1:1 onto Quality Runner and the Agent Eval Contract). Tracker #106, `reports/054-netic-2026-07-24.md`, PDF `output/cv-jakye-amos-netic-2026-07-24.pdf`. Not submitted — awaiting user review.
- Amazon applied + queue cleared on 2026-07-25: tracker #105 (SDE – 2026 US, report 050) moved `Evaluated` → `Applied`, and all 157 Amazon-branded queue items (company matching `amazon|\baws\b|annapurna|kuiper`) were set to `skipped` — the status `queue-lib.mjs` preserves across refresh and `eligibleForSelection` hard-excludes, so they cannot resurface. Twitch and Evi Technologies were deliberately kept as separate pipelines per user scope choice; the 115 opaque Amazon entities (ADCI India, ASSPL, Souq, Q-Tech Egypt) were already `excluded`/`stale`. At the time, today's selection held 5: N26, Sigma Computing, Evi, Anthropic, Twitch (superseded by 10-role refill — see top). Active queue 646 → 589. `queue.mjs verify` and `verify-pipeline.mjs` clean.
- Human-controlled application packets are browser-first and read-only, with exact `ready-for-human-review`, `needs-user-input`, `blocked`, and `stale` statuses; stable current/history artifacts; versioned confirmed answer references; audited resume reuse/tailoring; raw/humanized/approved narrative states; and a manual final Submit/Apply boundary. Dry runs now remain fully write-free, including packet history, while the 8 AM launcher remains source/queue-only and never generates packets or application artifacts.
- Added a recoverable posting-aging lifecycle in `queue-aging.mjs`: queue items now preserve `firstSeenAt`/`lastSeenAt`, down-rank after 14 days, flag `recheck_due` after 30, move unverified non-alert roles to `stale` after 45, and archive them after 60 without deleting history. Successful daily refreshes retain unseen roles long enough for this policy, source failures suspend transitions, and a later source observation reactivates the same queue item. Stale or archived roles cannot generate new submission packets until refreshed and revalidated.
- Added TeamWork Online as a second daily intake path alongside the existing Gmail alert source: the ignored local `portals.yml` now scans the public Sports Technology category through the versioned `teamworkonline` provider. The provider is read-only, host-pinned, capped at 8 pages, follows public pagination, and excludes configured Senior/Staff/Principal/Lead/Director/Manager/Intern/Part Time levels before the normal scanner filters. The live dry run on 2026-07-21 scanned 30 listings and produced 2 eligible offers.
- Resume state as of 2026-07-21: a `sports_analytics` lane (BBDSE/CourtIQ, Court Vision, BidCamp) is wired through `config/profile.yml`, `resume-contract.mjs`, and `apply/application-artifacts.mjs`; the sports base resume (`output/canonical-sports.html`) leads with the Claude-powered BidCamp RFP work and names the published AI/agent packages; and "Spring 2027" is removed everywhere resume-facing (cv.md, profile.yml, modes, artifact templates, and the 8 already-generated per-company resumes) because it undercut immediate availability.
- Purged `cv.md`, `article-digest.md`, and `interview-prep/story-bank.md` (real name/email/phone/address/career history) from git history on every branch of the `origin` fork (main, dev, chore/restore-scanner-subsystem, codex/career-materials-refresh, backup-pre-update-1.1.0, backup-pre-update-1.2.0) via `git-filter-repo`, force-pushed the rewrite (2026-07-21), and added all three to `.gitignore` — they now exist only as local untracked files. `upstream` (santifer/career-ops) was confirmed never contaminated. The 3 dependabot branches + 1 release-please branch (bot-managed) were deleted from `origin` rather than rewritten; GitHub Actions will recreate clean ones as needed. Origin now has exactly 6 branches, all purged.
- Firecrawl-backed public contact discovery and post-application outreach remain active; commits `cdca636` and `b8d6317` run a bounded, read-only `discover-queue` pass during every non-dry-run `queue.mjs refresh` after jobs are imported. Application selection remains independent from contact discovery: the UI/application flow selects 6 roles while discovery accepts its own batch limit, defaulting and clamping to 20. Selected eligible roles run first, then roles without snapshots; each attempted role stores public/Gmail header/ID evidence, conventions, contacts, unverified hypotheses, and provider errors in queue JSON/Markdown/UI, and remaining due roles stay queued.
- Queue `Applied` records are retained as unconfirmed; only an explicit local `submitted` result or a role-correlated Gmail confirmation with explicit evidence promotes the record to outreach-eligible. Signals remain one idempotent record per company and role.
- Email is restricted to `jakyejobs@gmail.com` and the explicit send path; LinkedIn output remains a manual draft. The local profile opts into evidence-backed convention hypotheses, which remain labeled unverified and guessed rather than `emailVerified`.
- Initial live-email ramp is capped at two messages per day until explicitly completed.
- Gmail OAuth is verified for `jakyejobs@gmail.com`, email outreach is enabled, and the 8:00 AM local queue/UI launch…

_(truncated for length)_

## Current Position

- Contact candidates are accepted only with public professional evidence or an evidence-backed convention hypothesis when the profile opt-in is false; ad-hoc guessed, private, personal, unrelated, and duplicate contacts are rejected. Queue refresh now attaches a cacheable discovery snapshot to each eligible `ready`/`in_review` role before application preparation, with the 6-role application selection cap decoupled from the 20-role discovery batch.
- Convention-derived addresses use local-part patterns from at least two named public examples across distinct source URLs, preserve sample coverage/confidence and evidence URLs, and can enter the sendable contact/outbox path when `requireVerifiedPublicEmail: false`; they remain `emailVerified: false`, `guessed: true`, and visibly unverified. The strict default still requires exact public or first-party Gmail-header verification. When no convention exists, the processor searches up to four named candidates with two bounded exact-name queries each; it still requires the exact person, employer domain, and allowed public evidence.
- The processor searches for public recruiter, hiring-manager, and team evidence plus verified first-party Gmail relationships on queue import and after application signals; `data/outreach-contacts.json` remains a supplement/correction manifest.
- Warm-network discovery reads only Gmail headers/IDs, never stores message bodies, and uses configured Amazon/CWRU sources to find relevant public recruiter, hiring-manager, and team signals without scraping LinkedIn or auto-messaging.
- Dogfood found and fixed a result-preservation defect in `cbb62fd`: scraped/search contact candidates now survive deduplication and retain search-result identity metadata.
- Discovery provider errors now retain prior candidates while recording fresh counts separately and keeping failure retries short; queue snapshots surface the failure and retry time rather than silently passing. Contact selection prioritizes verified emails before manual-only profiles while retaining one-recruiter/one-hiring-manager diversity.
- The autonomous application worker preserves fail-closed submission states and triggers outreach only for confirmed `submitted` results; queue `Applied`, dry runs, blocked, failed, anti-spam, and `submission_unknown` outcomes do not trigger it.
- Outreach sends use a durable deterministic outbox with Gmail idempotency headers, pre-send sent-message reconciliation, bounded retries after uncertain provider results, ramp/daily limits, and explicit `accepted by Gmail` versus delivery-unknown reporting.
- The Ramp test is retained as `blocked_by_antispam` with sanitized evidence; Deepgram remains `submission_unknown` and was not retried.
- The queue UI exposes submission-confirmation state, outbox/delivery status, verified-email state, follow-up dates, and LinkedIn drafts.
- Follow-ups are scheduled five business days after a send and stop on replies, bounces, opt-outs, rejection, closed roles, or pause.
- ATS adapters now select an approved accomplishment from the project ledger using the queue lane and job description; explicit scoped answers in `data/application-question-ledger.json` override that selection.
- Application answer resolution now covers the AI current-use/experiment family plus agentic/LLM-powered systems, shipped production software, customer demos/POCs, Python/JavaScript/systems foundations, cloud/container/basic-Kubernetes experience, and Python production projects; recent employer, recent title, earliest-start, Anthropic interview, Glean relationship, hybrid-work, geography, Sentry, LLM evaluation, code-recency, language, main-language, and pronoun prompts use explicit profile answers. Evidence-backed answers retain source references, geography families stay distinct, referral variants collapse into one unresolved source prompt, and privacy/policy/double-check acknowledgements are routed to manual review instead of the question ledger. Legal, eligibility, consent, and company-specific narrative prompts remain human-only.
- The queue UI exposes `Prepare today's packets`, application progress, canonical question answers, handoff tabs, and sanitized application status through `/api/applications/packets`, `/api/applications/packet`, `/api/applications/status`, `/api/questions/answer`, `/api/handoffs`, and `/api/handoffs/open`; it reuses the existing queue tab/server surface and leaves the final Submit/Apply action to the user.
- Completed the corrected queue-wide question-ledger dogfood pass on 2026-07-22: 133/133 active supported postings were replayed read-only from a 1,153-item queue. The final resolver reduced required unresolved instances from 331 to 259 and moved packet status from 33 ready/100 needing input to 34 ready/99 needing input. Every observed instance of the new answer families resolved: agentic 18/18, production 6/6, customer delivery 9/9, technical foundations 3/3, cloud 3/3, recent employer 7/7, recent title 7/7, and availability 10/10. The local ledger is now compacted to 187 canonical observations after removing historical duplicates and non-question acknowledgements; US, North America, and Bay Area location prompts are separate entries. Sensitive, eligibility, identity, and consent fields remain human-only, and all packet boundaries stayed human-only.
- `queue.mjs verify` remains structural; `queue.mjs health --limit 100` is a dry-run freshness report, while `queue.mjs health --all --apply` performs the explicit queue-wide stale update. LinkedIn and TeamWork Online alert URLs are never crawled.
- The application queue remains browser-gated for rendered-form inspection, but packet inspection is read-only: it does not read current values, fill controls, upload files, click options, or submit. The browser-free description path remains separate and does not derive or execute login, CAPTCHA, form-fill, or submission requests.

## Next Step

1. Review the corrected ledger and the staged packet report, then complete unresolved user-input questions through the queue UI; only evidence-backed normal answers are reusable and the final Submit/Apply action remains manual.
2. Use `Prepare today's packets` or `node apply/application-packets.mjs --queue-id <id>` when a selected supported-ATS role is ready; review the generated Markdown/JSON packet and attach the grounded artifacts.
3. Complete any question cards in the queue UI; answers update the canonical ledger and are reused for compatible future questions without auto-submitting. Review each packet and perform the final application action yourself.
4. Use the explicit Ramp handoff only if you want to complete the application manually after solving reCAPTCHA: `node application-queue.mjs handoff --queue-id 088a7ba9f8ac89514a74 --timeout 600`.
5. Keep accomplishment answers current by adding only verified outcomes/status changes to the project ledger; do not replace the lane selector with a single global answer.
6. Run `node queue.mjs refresh` during the next daily cycle to refresh Gmail and direct TeamWork Online intake together; the bounded read-only contact pass can process up to 20 due eligible roles independently of the 6-role application selection, while any deferred roles remain due for a later refresh. Review offers through the existing queue and human packet flow.
7. Let the next healthy queue refresh populate posting freshness metadata; use `node queue.mjs health --all --apply` when a role is due for explicit ATS/HTTP liveness verification.
8. Treat the current Amazon duo as LinkedIn drafts only; a confirmed submission is still required before any email path can become eligible. Convention hypotheses may now enter the local outbox under the profile opt-in, but remain visibly unverified and require the explicit send path; never probe SMTP.

## Validation

- Focused packet/ledger/form/artifact/Jack tests pass (41), queue/policy/aging/launcher tests pass (19), and `verify-pipeline.mjs`, syntax checks, `git diff --check`, and the 8 AM launchd dry-run pass. OpenCLI doctor still reports the browser bridge unavailable and macctl doctor still reports its daemon/socket unavailable, so no live authenticated-browser acceptance is claimed.
- The adapter/application syntax checks, `git diff --check`, anti-spam detection tests, queue handoff guard, and state readback pass.
- The 2026-07-22 authorized Amazon and SeatGeek discovery dogfood completed read-only: Amazon returned nine recruiter/manager candidates and no eligible address; SeatGeek returned eight current recruiter/engineering candidates and no eligible address after eight bounded exact-name/domain searches. Gmail header/ID discovery found no qualifying relationship address, and the SeatGeek run rejected one code/configuration pseudo-identity before convention inference. No convention was observed and both runs sent nothing. Stale alumni, blocked-source provenance, pseudo-identities, and ATS-domain handling were fixed and rechecked against SeatGeek.
- The final staged-packet audit used 133 isolated packets and kept the source queue hash unchanged. All new answer-family instances resolved with no sensitive evidence answers, no profile-address false positives, zero navigation actions, and `human-only` submission boundaries; the remaining blockers are referral-source values, company-specific motivation, real legal/eligibility/consent fields, unresolved role-specific language or relocation requirements, and genuinely role-specific questions. Dutch C1/C2 proficiency is now user-confirmed `No`, and privacy/policy/double-check labels are no longer treated as unanswered questions. No application was submitted and no email was sent.
- Commits `cdca636` and `b8d6317` validation: focused queue/discovery/application tests pass, the full JavaScript suite passes 150 with 6 environment-skipped browser tests (156 total), plus syntax and diff checks. A live `node outreach.mjs discover-queue --force --limit 1` run made only Firecrawl/Gmail read-only attempts, recorded Firecrawl DNS/Gmail fetch errors on the queue item, exited nonzero, and left the outreach outbox at 0.
- JavaScript syntax checks, `git diff --check`, queue verification, application status, project-ledger source validation, and the no-network application clear and queue health dry runs pass; neither dry run submitted applications or sent outreach.
- A live read-only smoke against the current Vercel Greenhouse queue item returned 6,585 description characters from `https://boards-api.greenhouse.io/v1/boards/vercel/jobs/6105394004`; no browser or submission path was used.
- Doctor reports the existing Playwright Chromium installation issue and Playwright MCP warning; the adapters can still target the user’s installed Chrome Beta for visible handoffs, but a bundled Chromium install remains a prerequisite for the fallback browser path.
- `cv-sync-check.mjs`, `resume-audit.mjs`, and the `resume-audit`/`resume-contract` test suites pass against the rewritten full `cv.md`.
- The 2026-07-22 career-source refresh passed `pnpm social:verify`, `pnpm sync-check`, `resume-audit.mjs --cv cv.md` (one existing year-only-date warning), JSON/YAML parsing, and the focused project-ledger/social tests (8/8).
- The full JavaScript suite passes 169 with 6 environment-skipped tests (175 total); focused corrected packet/ledger tests, JSON/YAML parsing, syntax checks, and `git diff --check` remain green. Existing outreach dry-run and no-submit/no-send boundaries remain green; no email was sent or application submitted during this work.
- A live read-only Deepgram Ashby packet smoke captured 15 exact application questions, zero unresolved required questions, and 3 human-only fields; it reused existing sponsorship, Terraform, SRE, and Kubernetes answers without creating duplicate canonical entries. Artifact-enabled packet generation produced one-page resume and cover-letter PDFs from the ATS-enriched description. No control was filled, clicked, uploaded, or submitted.
- New script `generate-full-cv.mjs` renders `cv.md` verbatim (no lane truncation) to `output/Jakye_Amos_Full_CV.pdf` via the same `renderHtmlToPdf` ATS-safe pipeline `apply/application-artifacts.mjs` uses, reusing `templates/cv-template.html`'s CSS; it intentionally bypasses `generate-pdf.mjs`'s CLI-only section-order check (mirroring how the tailored-resume path already calls `renderHtmlToPdf` directly) since the full CV keeps Skills near the top rather than the tailored templates' Skills-last convention.
- `output/canonical-base.html` (source of `output/Jakye_Amos_Canonical_Base_Resume.pdf`, the default artifact `resume-contract.mjs` points to) is a fully-rendered static file, hand-edited, not template-driven. Refreshed its Skills lines to match `cv.md` (added R, MATLAB, Express, tRPC, SQLite, Git) and appended "Amazon Future Engineer Scholar (2022)" to the Education line; re-rendered via a direct `renderHtmlToPdf` call (same order-check bypass as above) — still fits on 1 page with margin to spare. Any per-application `resume-manifest.json` that had hash-pinned the old PDF will now correctly detect it as stale on next use.
