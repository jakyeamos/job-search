name: 'CodeQL Analysis'
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 4 * * 1' # Weekly Monday 4am UTC

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        language: ['javascript-typescript', 'go']
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4.37.3
        with:
          languages: ${{ matrix.language }}

      - name: Setup Go
        if: matrix.language == 'go'
        uses: actions/setup-go@v6
        with:
          go-version-file: dashboard/go.mod
          cache-dependency-path: dashboard/go.sum

      - name: Build Go
        if: matrix.language == 'go'
        run: cd dashboard && go build ./...

      - name: Autobuild
        if: matrix.language != 'go'
        uses: github/codeql-action/autobuild@v4.37.3

      # This is a private repo without GitHub Advanced Security / code
      # scanning enabled, so the SARIF upload here 403s regardless of
      # analysis results. Non-blocking until code scanning is enabled in
      # repo settings (Settings -> Code security and analysis).
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4.37.3
        continue-on-error: true
        with:
          category: '/language:${{ matrix.language }}'
